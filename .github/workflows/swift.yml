name: "Build & Run Tests"
on: [push]
jobs:
  build:
    runs-on: macos-11.0
    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: swift build -v
    - name: Run Tests
      run: swift test -v
